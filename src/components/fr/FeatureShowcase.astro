---
// src/components/FeatureShowcase.astro
// Inspired by the "Meet CropTab" sticky showcase
---
<section id="features" class="bg-[var(--color-ivory)] text-[var(--color-noir)] relative overflow-hidden py-32">
  <div class="max-w-7xl mx-auto px-6 md:px-12 flex flex-col items-center">
    
    <div class="text-center mb-24 reveal-feature" data-speed="0.9">
      <p class="font-accent text-[var(--color-bordeaux)] tracking-widest uppercase text-sm mb-4">The Brand of the Tower</p>
      <h2 class="font-display text-5xl md:text-6xl lg:text-7xl text-[var(--color-ivory)] leading-[1.05] mb-6">
        Design Sans<br/><span class="italic text-[var(--color-gold)]">Compromis</span>
      </h2>
      <p class="font-body text-[var(--color-silver)]/60 text-lg max-w-xl">
        Chaque détail est conçu pour servir un seul objectif : la conservation parfaite de votre collection.
      </p>
    </div>

    <!-- Parallax Decorators -->
    <div class="absolute top-[10%] left-[5%] w-64 h-64 bg-[var(--color-oat)] rounded-full blur-[100px] pointer-events-none opacity-50 parallax-elem" data-speed="1.2"></div>
    <div class="absolute bottom-[20%] right-[10%] w-96 h-96 bg-[var(--color-gold)]/20 rounded-full blur-[120px] pointer-events-none parallax-elem" data-speed="0.7"></div>


    <!-- Features block, similar to Meet CropTab layout -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-16 md:gap-32 w-full relative">
      
      <!-- Central line divider for desktop -->
      <div class="hidden md:block absolute left-1/2 top-4 bottom-4 w-px bg-gradient-to-b from-transparent via-[var(--color-graphite)]/20 to-transparent -translate-x-1/2"></div>
      
      <!-- Feature 1: Left -->
      <div class="flex flex-col md:text-right w-full md:items-end justify-center reveal-feature pb-12 parallax-elem" data-speed="0.95">
        <div class="w-12 h-12 rounded-full border border-[var(--color-gold)] flex items-center justify-center mb-6 text-[var(--color-bordeaux)] bg-white shadow-[var(--shadow-gold)] relative">
          <!-- Sound icon -->
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-mic-off"><line x1="2" x2="22" y1="2" y2="22"/><path d="M18.89 13.23A7.12 7.12 0 0 0 19 12v-2"/><path d="M5 10v2a7 7 0 0 0 12 5"/><path d="M15 9.34V5a3 3 0 0 0-5.68-1.33"/><path d="M9 9v3a3 3 0 0 0 5.12 2.12"/><line x1="12" x2="12" y1="19" y2="22"/></svg>
        </div>
          <h3 class="font-display text-3xl md:text-4xl text-[var(--color-ivory)] mb-4">Absolument Silencieux</h3>
          <p class="font-body text-[var(--color-silver)]/60 leading-relaxed">
            Des compresseurs amortis importés de l'ingénierie aérospatiale. Le « Silence Caveduke » ne dépasse pas 38 db — moins qu'un murmure dans une bibliothèque.
          </p>
      </div>

      <!-- Spacer for grid -->
      <div class="hidden md:block h-32"></div>
      <div class="hidden md:block h-32"></div>

      <!-- Feature 2: Right -->
      <div class="flex flex-col w-full justify-center reveal-feature pt-12 parallax-elem" data-speed="1.05">
        <div class="w-12 h-12 rounded-full border border-[var(--color-gold)] flex items-center justify-center mb-6 text-[var(--color-bordeaux)] bg-white shadow-[var(--shadow-gold)] relative">
          <!-- Drop icon -->
          <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-droplets"><path d="M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7 2.9 7 2.9s-2.01 3.23-2.9 4.36S2.5 11 2.5 12.25c0 2.22 1.8 4.05 4.5 4.05z"/><path d="M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 1.22 2 3.38 3.27 5.09s2.47 3.51 2.47 5.2a5.5 5.5 0 0 1-11 0c0-1.2.53-2.73 1.5-4.11a14.71 14.71 0 0 1 2.32-2.6z"/></svg>
        </div>
          <h3 class="font-display text-3xl md:text-4xl text-[var(--color-ivory)] mb-4">Contrôle Hygrométrique 65%</h3>
          <p class="font-body text-[var(--color-silver)]/60 leading-relaxed">
            Chambre en verre Climalit à double isolation qui maintient l'humidité relative dans la plage exacte exigée par les grands crus.
          </p>
      </div>

    </div>
  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const features = document.querySelectorAll('.reveal-feature');
  
  features.forEach(feature => {
    gsap.fromTo(feature, 
      { y: 50, opacity: 0 },
      {
        y: 0,
        opacity: 1,
        duration: 1,
        ease: 'power3.out',
        scrollTrigger: {
          trigger: feature,
          start: 'top 95%',
        }
      }
    );
  });

  // Advanced Parallax logic
  const parallaxElems = document.querySelectorAll('.parallax-elem, [data-speed]');
  parallaxElems.forEach(elem => {
    // @ts-ignore
    const speed = parseFloat(elem.dataset.speed || '1');
    const yVal = (1 - speed) * 300; // The difference from base speed 1
    
    gsap.to(elem, {
      y: yVal,
      ease: 'none',
      scrollTrigger: {
        trigger: '#features',
        start: 'top bottom',
        end: 'bottom top',
        scrub: true
      }
    });
  });
</script>
